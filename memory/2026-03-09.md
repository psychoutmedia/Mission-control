# March 9, 2026

## Heartbeat Work

### Task Queue Progress
- Added 3 tasks to queue:
  - Set up Chroma vector DB (DONE)
  - Build async agent loop example (DONE)
  - Deploy RAG chatbot to Docker
- Installed chromadb via pip

### Chroma RAG Demo Built
- Created `projects/rag_chatbot/chroma_rag.py`
- Uses Chroma for persistent vector storage
- Database: `projects/rag_chatbot/knowledge/chroma_db/`

### Async Agent Loop Demo
- Created `projects/async_agent/agent_loop.py`
- Single async agent with tool use
- Concurrent multi-agent execution
- Agent pipeline (sequential stages)
- Uses asyncio for parallel operations

### Docker Deployment Ready
- Created `Dockerfile` for RAG chatbot
- Created `docker-compose.yml` (API + Streamlit services)
- Created `requirements.txt` (updated with FastAPI)
- Created Streamlit UI (`app.py`)

### FastAPI Server
- Created `api.py` with REST endpoints

### LeetCode Practice
- 3 medium problems practiced:
  1. Valid Parentheses (stack)
  2. Longest Substring Without Repeating (sliding window)
  3. Container With Most Water (two pointers)
  - `GET /` - Root
  - `GET /health` - Health check
  - `POST /chat` - Chat with RAG
  - `GET /search` - Search knowledge base
  - `POST /clear` - Clear conversation
  - `GET /conversation` - Get history
- Run with: `uvicorn projects.rag_chatbot.api:app --reload`

### Dependencies Installed
- chromadb + dependencies
