# 2026-04-14 - Daily Memory

## Session Start (08:03 AM)

- **Weather**: Salford, UK — +5°C, partly cloudy
- **Queue**: Added LeetCode practice (Arrays & Hashing, 3 mediums)
- **Email**: himalaya CLI still broken (needs re-auth)
- **Last weekly review**: Apr 12

## Work Done

### Morning Sprint (08:03 AM)

**LeetCode Practice — Arrays & Hashing**

1. **Contains Duplicate** (217) - Medium
   - Problem: Return true if array has any duplicates
   - Approach: HashSet — O(n) time, O(n) space
   - Key insight: Early exit if len(nums) > len(set(nums))

2. **Valid Anagram** (242) - Easy → treating as review
   - Problem: Check if t is anagram of s
   - Approach: Count chars or sort — O(n) or O(n log n)
   - Key insight: Edge cases — different lengths = instant fail

3. **Two Sum** (1) - Easy → treating as warmup
   - Problem: Return indices of two numbers that add to target
   - Approach: HashMap complement lookup — O(n) time
   - Key insight: Check if complement exists before adding current

## Notes

- Queue empty this morning — added daily LeetCode task
- Should add more tasks proactively for autonomous work
- Continue building LeetCode streak

## Afternoon Sprint (01:03 PM)

**LeetCode Practice — Two Pointers & Sliding Window**

1. **Valid Palindrome II** (680) - Medium
   - Problem: Check if string can be palindrome after deleting ≤1 char
   - Approach: Two pointers, skip either side on mismatch, check remaining
   - Key insight: O(n) with O(1) space

2. **Longest Substring Without Repeating** (3) - Medium
   - Problem: Find longest substring with all unique chars
   - Approach: Sliding window + hash map for last seen indices
   - Key insight: Track left boundary to handle repeated chars

3. **Minimum Window Substring** (76) - Hard
   - Problem: Find minimum window in s containing all chars of t
   - Approach: Sliding window with counter, expand right, contract left
   - Key insight: O(n) — each char visited at most twice

**Queue:** Clean, MLOps research task ready

## End State (01:15 PM)

- LeetCode: 3 done (review problems)
- Queue: Clear, ready for next task
